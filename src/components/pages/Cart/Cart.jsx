// @flow
import * as React from 'react';
import styles from './Cart.module.css';

export const Cart = (props) => {
  return (
    <div className={styles.not_found_box}>
      <h1>Cart page is in construction!</h1>
    </div>
  );
};
